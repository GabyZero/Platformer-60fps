#ifndef RESOURCES_ENUM_HPP
#define RESOURCES_ENUM_HPP

namespace resources{
    namespace enums{
        
        enum Players {PLAYER_DEFAULT};
        enum Blocks {BLOCK_DEFAULT};
        enum Backgrounds {BACKG_DEFAULT};

    } // namespace Enums
}
#endif